# 交接说明（换新会话先读这份）

> 给「新开一个对话框继续改这个项目」用。读完这份 + `AGENTS.md`，就不需要旧会话的记忆了。
> 最后更新：2026-09-24。

## 0. 一句话

饭饭 · 今天吃啥（fanfan-web）：单文件、纯前端的「今天吃什么」智能体，手机浏览器直接打开就能用。无框架、无构建、无 npm 依赖。

## 1. 文件地图与同步规则

| 角色 | 位置 |
| --- | --- |
| 工作区正本（我这边改的是这个） | `C:\Users\24488\Documents\Codex\2026-09-15\ge\outputs\index.html` |
| git 仓库（真正提交/推送的） | `D:\饭饭web\` |
| 必须逐字节一致的副本 | `D:\饭饭web\index.html`、`今天吃啥.html`、`分享给朋友\{index,今天吃啥}.html`（共 4 份） |
| 去密钥版（生成物，不要手改） | `D:\饭饭web\备用-不含密钥版\{index,今天吃啥}.html`，由 `开发脚本\make-share.mjs` 从正本生成 |
| 菜单库数据 | `data\shops.json`（严格 JSON）+ `商家数据库\解析结果\`（入库流水账） |
| 开发脚本（约 40 个零依赖 mjs） | `开发脚本\` |
| 文档 | `README.md`、`AGENTS.md`、`标签规范.md`（写菜品数据前必读）、`方案-LLM主导Agent改造.md` |

改代码的固定动作：改正本 → 拷到那 4 个副本 → 跑 `make-share.mjs` 重新生成去密钥版 → 提交推送。

## 2. 发版流程（每次改完照做）

1. 跑测试：`check-syntax.mjs` + 受影响的 `test-*.mjs`（见第 6 节）。
2. 同步 4 份副本：把正本 `Copy-Item` 到 `D:\饭饭web\index.html`、`今天吃啥.html`、`分享给朋友\index.html`、`分享给朋友\今天吃啥.html`。
3. 生成去密钥版：`node 开发脚本\make-share.mjs <正本> D:\饭饭web\备用-不含密钥版`。
4. 提交：`git -C D:\饭饭web add -A`，然后 `git -C D:\饭饭web commit -m "单行中文说明"`。
5. 推送：**不能用 git push**（`github.com:443` 经常被重置），走 `开发脚本\push-via-api.mjs`。令牌在 Windows 凭据管理器里，用 PowerShell P/Invoke `CredRead` 读，目标名 `GitHub - https://api.github.com/zhemgcm-maker`，**按 UTF-8 解码**（得到 40 字符的 `gho_…`）。用法：`$env:GH_TOKEN=<令牌>; node 开发脚本\push-via-api.mjs "D:\饭饭web" "zhemgcm-maker/fanfan-web"`。
6. `api.github.com` 也会间歇性连接超时，失败就循环重试（实测要 2～5 次）。
7. 验证上线：轮询 `https://zhemgcm-maker.github.io/fanfan-web/?t=<时间戳>`（用 `curl.exe -s --compressed`，别用 Invoke-WebRequest 的 Contains 判断压缩内容），直到能搜到这轮新增的代码标记。Pages 部署约 30～120 秒。

## 3. 环境约束（踩过的坑）

- 没有 `grep`/`sed`，用 `Select-String`；shell 是 PowerShell。
- PATH 里没有 `node`，用绝对路径：`C:\Users\24488\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`。
- 开发脚本一律 `node 脚本.mjs <index.html 路径>`，参数取 `process.argv[2]`。
- 测试脚本里的假 DOM 是手写桩：`querySelector` 带缓存、`addEventListener` 可能是空实现，写断言时注意。

## 4. 架构速览

两条决策链，**默认走 Agent 模式**，无网 / 没 Key / 失败时自动回退算法引擎：

1. **Agent 模式**：`AGENT_TOOLS` 12 个工具（读记忆 / 搜菜 / 体检 / 搜店 / 查菜单 / 配桌 / 定案…），`agentRun()` 是 harness 主循环，`AGENT_MAX_STEPS = 12`，`finalize` 有闸门（id 白名单、忌口、预算）。
2. **算法引擎**：本地打分（距离 / 评分 / 菜系 / 覆盖 / 人均 / 口味 / 用餐方式），可复现、离线可用。

**工具调用有两种协议**，`agentProtocol()` 自动选：`native`（真把 `tools` 发出去，模型回 `tool_calls`，直连 DeepSeek 与 2026-09-23 之后的后端代理都是这种）和 `text`（通道吞 `tools` 时把工具清单写进提示词，要求模型只回一行 `{"tool":"…","args":{…}}`，本地解析还原）。

协议结论自动维护：`ensureAgentChannel()` 在结论缺失 / 超过 6 小时时补一次 `ping_tool` 探测；万一按原生跑而模型第一步没动静，`agentRun()` 会自动换文字协议重跑并把结论改回来 —— **不需要用户点自检**。

**菜单库三层作用域**：`branch` 本店确认（高德 ID 精确绑定）＞ `brand` 品牌参照（同品牌同城共用）＞ `cuisineRef` / `cuisineRefKeys` 菜系参照（同城同类店借菜单，只做加法、不当封闭菜单）。菜系参照里还有个开关 `cuisineRefWhole: true`：被 `cuisineRefKeys` 关键词命中的**同类店**可以整份借这份菜单，不再逐道比菜的菜系——肉蟹煲这类菜只能这么挂（高德给肉蟹煲店的是"中餐厅"，程序推成「家常」，菜却按江浙/川写，逐道比就永远借不到）。只有显式开了这个开关的记录才放宽，别的参照仍按老口径，不会跨菜系串味。

**标签体系**见 `标签规范.md`（界面标签 → 关键词 → 菜品 tags 三层；忌口 `alg` 只认 15 个 token）。

**高德类型 → 菜系**（`AMAP_CUI_RULES` / `amapCui`）：规则是"拿 type+店名整串从上往下第一个命中"，所以**具体菜系必须排在通用兜底之前**（保定 / 河北 / 家常 / 中餐厅 / 小吃放最后）。2026-09-25 重排过一次，修掉三个连锁判错：胖哥俩（type 浙江菜、店名带保定）原来判成「保定」、粤扬楼淮扬菜（type 淮扬菜）判成「家常」、沙县小吃（type 小吃快餐）判成「西」。判错的后果不是"显示不准"，而是 `restaurantServes` / `shopCanMake` 认为"这家店做不了那道菜"，Agent 被 finalize 闸门反复驳回、把步数烧完再退回算法。改这里务必跑 `test-shop-db.mjs`（末尾有一组真实 type 的回归用例）。

## 5. 数据现状（2026-09-25）

- 菜品库 `DISHES`：**712 道**，25 个菜系（家常最多，川、粤次之）。
- 菜单库 `data\shops.json` 5 家店：女掌柜土家菜馆（branch，B0FFGWFUGT，93 道）、川香苑（branch，B0K05HVF6L，26 道）、沙县小吃（brand，53 道）、熊麻婆现炒浇头面·饭（branch，B0MUH1QNEC，30 道）、胖哥俩肉蟹煲（brand + B0JUGD3FC6 绑万博广场店，15 道）。
- 保定「肉蟹煲」类店共 11 家（钳哆、多嘴超级、猫小猫家、赖胖子、蟹逅、胖哥肉蟹煲…），都没采集过菜单，靠胖哥俩那份的 `cuisineRefWhole` 认菜。
- 用户常驻位置：华北电力大学保定二校区 `115.514611,38.888900`。
- 线上：https://zhemgcm-maker.github.io/fanfan-web/
- 队友搭的后端（阿里云 FC）：`https://fanfan-web-qpznnriewb.cn-hangzhou.fcapp.run`，接口 `/api/llm`（已透传 tools）、`/api/amap`、`/api/register`、`/api/login`、`/api/profile`、`/api/settings`。

## 6. 测试脚本怎么跑

```powershell
$node = "C:\Users\24488\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
& $node 开发脚本\check-syntax.mjs index.html      # 每次必跑：语法 + 重复 id + 缺失 DOM id
& $node 开发脚本\test-server.mjs index.html       # 后端融合（离线断言，很快）
& $node 开发脚本\test-locate.mjs index.html       # 定位 / 选地点 / 模糊联想
& $node 开发脚本\test-shop-db.mjs index.html      # 商家数据库与菜系参照
& $node 开发脚本\test-memory-edit.mjs index.html  # 「我的」AI 记忆的编辑 / 删除 / 撤销 + 最近吃过日期
& $node 开发脚本\smoke-ui.mjs index.html          # 全交互回归，约 90 秒
& $node 开发脚本\e2e-agent-ui.mjs index.html      # 真连 DeepSeek 跑 Agent（慢，受网络影响）
```

`smoke-ui.mjs` 里固定 `agent='off'` 是刻意的（否则真连大模型、结果随机），别改回去。

## 7. 最近做过什么（时间倒序，细节看 git log）

1. 修「勾苏浙菜 → 步数烧完退回算法」：根因是高德类型→菜系映射判错（见第 4 节），江浙菜馆被推成「保定/家常」，于是"这家店做不了这道菜"，finalize 一路驳回。除了重排规则，还加了四道保险：`list_shops_for_dish` 每条店标注 `can_make`（并说清"can_make=true 的才做得了"）、`build_combo` 返回 `anchor_used`（锚定菜没进桌就明确写"别把它写进 finalize"）、finalize 被驳回时给可照做的 hint、第 11 步起每轮硬提醒"只剩 N 轮，立刻交方案"，步数上限 12→14。实测同一条需求：修复前 12 步压线、14.8 秒、prompt 11.3 万 token、结果还不是苏浙菜；修复后 7 步、10.2 秒、5.2 万 token，交出「江苏盐水鸭 + 米饭 + 红豆酒 @粤扬楼淮扬菜」；吃好饭档、吃面档、湘菜也都能在 6~8 步内给出对口的方案。
2. 采集入库「胖哥俩肉蟹煲」：15 道新菜进菜品库（`pg01`–`pg15`，煲类 other+main / 小食凉菜 other+side，菜名照抄菜单含「笑薯你了」这种营销前缀），商家数据库里加一份 15 道的统一菜单，挂 `brand` + `amapId B0JUGD3FC6`（万博广场店），同名品牌另一家分店（未来石万达）按品牌名共用。为让保定的其他肉蟹煲店（钳哆/多嘴超级/猫小猫家/赖胖子/蟹逅/胖哥肉蟹煲）也能认这套菜，新增菜系参照开关 `cuisineRefWhole`（见第 4 节）。实测：胖哥俩两家 ✅、6 家同类店 ✅、普通家常馆 ❌ 不误伤。
3. 结果页的契合度条：`.scorebar .track/.fill` 原来是 `<span>`（行内元素 width 不生效），条子一直是空的灰条 —— 现在显式 `display:block`，条长 = 分数（满分 100，50 分填一半），数字封顶 100（不再出现"105 分满条"）。**配色试过"绿/橙/红三档"被用户否掉，已还原成原来的橙色渐变**；分母也还原成 90/20/23/100，别再往"按实测上限校准"的方向改回去了（2026-09-24 试过，用户不要）。
4. 点完「就吃这一桌」放一个「祝你用餐愉快」小特效（`#bless` 覆盖层：菜品表情上下晃 + 四颗 ✨🎉🥢🍜 飞散 + 卡片弹入，纯 CSS，无外链），2 秒后自动 `exitResultMode()` + `switchTab('home')` 回到「今天吃什么」；点一下特效可以立刻跳过。以前是记完就停在结果页，用户得自己找「重新选菜」。
5. 「我的 → AI 记忆」三块（喜欢 / 不喜欢、被屏蔽的推荐、最近吃过）都是入口：卡片里只放摘要，点整块进 `#pageMem` 全记录页（整页切走、底部 Tab 收起、靠「返回」回来）。全记录页里喜欢 / 不喜欢列全量、每条可删；被屏蔽的菜和店都写出名字，可逐条删 + 一键清空；最近吃过按"一顿一张卡"展示：日期大字（今年不带年份）→ 饭店名 + 时刻 → 菜品默认折叠（点开才列出每道菜和价钱，可逐道删）。一顿的组标识是 `history[].meal`（`acceptCombo` 写入，同一桌共用一个 ts），老记录没这个字段就按 45 分钟兜底分组；`acceptCombo` 还顺手记下 `shop`/`shopId`，所以新记的饭都能显示饭店名。删任何一条都是 6 秒内点提示条可撤销，相对天数按自然日算。
6. 后端 `/api/llm` 透传 `tools`：页面自动探测 + 自动切换协议，实测原生 function calling 11.5 秒出方案。
6. 选地点支持模糊联想：本地跳字匹配（"华电"→华北电力大学）+ 高德 `/v3/assistant/inputtips`；300ms 防抖、旧响应丢弃、拼音合成期不发请求、命中高亮；顺手用高德 POI 校准了三个城市的地标坐标。
7. 设置改完即存（服务商 / 模型 / API Key / 高德 Key / 后端地址 / 决策模式），状态行标出实际走哪条链路。
8. 结果页一步一屏 + 底部「重新选菜」；推理过程只留一行关键提示。

## 8. 待办 / 可能的下一次

- 楼下沙县小吃（高德 ID `B0H06MD6IH`，444 米）拍菜单照片 → `import-menu.mjs` 入库 → `bind-shop.mjs --amapId B0H06MD6IH` 绑成本店确认。
- 湘菜 / 西北菜缺"吃饭"类平价菜：2026-09-25 量过——西北菜 19 道、云贵 10 道里**一道 cat=rice（配米饭）的都没有**，湘菜只有 2 道（都是猪肝）。勾「西北菜 + 吃饭」会空手而归。
- 若要做「用户自带 Key 走后端（BYOK）」：后端改成"优先用请求头里的 `x-user-key`，没有才用服务端 Key"，前端加一个开关。
- 建议队友：后端 `/api/amap` 的高德 Key 换成「Web服务」类型（现在返 10002，被自动回退本机）。

## 9. 跟这位用户协作的注意事项

- 全程中文；**给结论要给证据**（实测数据、前后对比、失败案例），不要只给判断。
- 在意手机端体验：界面啰嗦、乱码、要一直往下滑这类问题优先级高。
- 文案要朴素，别自作主张加"炫技"的话（例如输入框写"打几个字就联想"被否过）。
- 每次改完的期待是：跑测试 → 推 GitHub → 给线上链接。
- 密钥：`DEFAULT_AMAP_KEY`、`DEFAULT_KEY` 明文写在 `index.html` 里，是仓库主的明确选择（后果自负）；**不要在别的文件、日志、文档里再复制这些值**，本仓库是公开的。
