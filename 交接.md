# 交接说明（换新会话先读这份）

> 给「新开一个对话框继续改这个项目」用。读完这份 + `AGENTS.md`，就不需要旧会话的记忆了。
> 最后更新：2026-09-25（这一版覆盖到"菜单批量入库"那一波结束）。

## 0. 一句话

饭饭 · 今天吃啥（fanfan-web）：单文件、纯前端的「今天吃什么」智能体，手机浏览器直接打开就能用。无框架、无构建、无 npm 依赖。

## 1. 文件地图与同步规则

| 角色 | 位置 |
| --- | --- |
| 工作区正本（改的是这个） | `C:\Users\24488\Documents\Codex\2026-09-15\ge\outputs\index.html` |
| git 仓库（真正提交/推送的） | `D:\饭饭web\` |
| 必须逐字节一致的副本 | `D:\饭饭web\index.html`、`今天吃啥.html`、`分享给朋友\{index,今天吃啥}.html`（共 4 份） |
| 去密钥版（生成物，不要手改） | `D:\饭饭web\备用-不含密钥版\{index,今天吃啥}.html`，由 `开发脚本\make-share.mjs` 从正本生成 |
| 菜单库（前端读的） | `data\shops.json`（严格 JSON）+ 页面里的 `SHOP_DB_BUILTIN` 快照（由 `build-data.mjs` 写入） |
| 菜单源文件（人工维护） | `商家数据库\解析结果\<店名>.json`，一家店一份 |
| 入库流水账 | `data\新菜记录.json`（现 270 条）、`data\新菜候选.json`（入库时自动生成） |
| 开发脚本（约 41 个零依赖 mjs） | `开发脚本\` |
| 文档 | `README.md`、`AGENTS.md`、`标签规范.md`（写菜品数据前必读）、`方案-LLM主导Agent改造.md` |

改代码的固定动作：改正本 → 拷到那 4 个副本 → 跑 `make-share.mjs` 重新生成去密钥版 → 提交推送。

## 2. 发版流程（每次改完照做）

1. 跑测试：`check-syntax.mjs` + 受影响的 `test-*.mjs`（见第 6 节）。
2. 同步 4 份副本：把正本 `Copy-Item` 到 `D:\饭饭web\index.html`、`今天吃啥.html`、`分享给朋友\index.html`、`分享给朋友\今天吃啥.html`。
3. 生成去密钥版：`node 开发脚本\make-share.mjs <正本> D:\饭饭web\备用-不含密钥版`。
4. 提交：`git -C D:\饭饭web add -A`，然后 `git -C D:\饭饭web commit -m "单行中文说明"`。
5. 推送：**不能用 git push**（`github.com:443` 经常被重置），走 `开发脚本\push-via-api.mjs`。令牌在 Windows 凭据管理器里，用 PowerShell P/Invoke `CredRead` 读，目标名 `GitHub - https://api.github.com/zhemgcm-maker`，**按 UTF-8 解码**（得到 40 字符的 `gho_…`）。用法：`$env:GH_TOKEN=<令牌>; node 开发脚本\push-via-api.mjs "D:\饭饭web" "zhemgcm-maker/fanfan-web"`。注意 commit message 里别带 ASCII 双引号（PowerShell 会当成 pathspec）。
6. `api.github.com` 也会间歇性连接超时，失败就循环重试（实测 2～5 次）。
7. 验证上线：轮询 `https://zhemgcm-maker.github.io/fanfan-web/?t=<时间戳>`（用 `curl.exe -s --compressed`，别用 Invoke-WebRequest 的 Contains 判断压缩内容），直到能搜到这轮新增的代码标记；再比一次 MD5 确认线上 = 本地正本。Pages 部署约 30～120 秒。

## 3. 环境约束（踩过的坑）

- 没有 `grep`/`sed`，用 `Select-String`；shell 是 PowerShell。
- PATH 里没有 `node`，用绝对路径：`C:\Users\24488\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`。
- 开发脚本一律 `node 脚本.mjs <index.html 路径>`，参数取 `process.argv[2]`。
- 测试脚本里的假 DOM 是手写桩：`querySelector` 带缓存、`addEventListener` 可能是空实现，写断言时注意。
- **页面里的 `const DISHES` / `SHOP_DB` 是脚本作用域，`window` 上没有**（函数才有）。真浏览器里验证只能走 `dishById()` / `dbShopFor()` / `shopCanMake()` 这些函数入口。
- 真浏览器验证用 `playwright`（装在 node runtime 的 node_modules 里，`channel:'msedge'` 可启动）；`page.evaluate` 里访问不到 `localStorage`。

## 4. 架构速览

两条决策链，**默认走 Agent 模式**，无网 / 没 Key / 失败时自动回退算法引擎：

1. **Agent 模式**：`AGENT_TOOLS` 12 个工具（读记忆 / 搜菜 / 体检 / 搜店 / 查菜单 / 配桌 / 定案…），`agentRun()` 是 harness 主循环，`AGENT_MAX_STEPS = 14`，`finalize` 有闸门（id 白名单、忌口、预算、店能不能做这道菜）。
   - 防"白烧步数"的三道保险：`list_shops_for_dish` 每条店标 `can_make`；`build_combo` 返回 `anchor_used`（锚定菜没进桌就写清"别写进 finalize"）；第 11 步起每轮硬提醒"只剩 N 轮，立刻交方案"。
2. **算法引擎**：本地打分（距离 / 评分 / 菜系 / 覆盖 / 人均 / 口味 / 用餐方式），可复现、离线可用。

**工具调用两种协议**由 `agentProtocol()` 自动选：`native`（真把 `tools` 发出去，模型回 `tool_calls`）和 `text`（通道吞 `tools` 时把工具清单写进提示词，要求只回一行 `{"tool":…}`，本地解析还原）。`ensureAgentChannel()` 在结论缺失 / 超过 6 小时补一次 `ping_tool` 探测，跑错了会自动换协议重跑。

**菜单库四层作用域**（由窄到宽）：

| 作用域 | 字段 | 含义 |
| --- | --- | --- |
| 本店确认 | `scope:'branch'` + `amapId` | 就是这家店，菜单是封闭集合（菜单里没有 = 不能做） |
| 品牌参照 | `scope:'brand'` | 同品牌同城分店共用，价格标"参考价" |
| 菜系参照 | `cuisineRef` / `cuisineRefKeys` | 同城同类店借菜单，**只做加法**、不当封闭菜单 |
| 同类店整份借 | `cuisineRefWhole:true` | 被 `cuisineRefKeys` 命中的店可整份借，不再逐道比菜的菜系（肉蟹煲/江西菜这种"店被判成家常、菜却按别的菜系写"只能这么挂） |

branch 记录同时会进 `byBrand` 索引，所以只采了一家分店时别的分店也能按品牌名兜底。

**标签体系**见 `标签规范.md`（界面标签 → 关键词 → 菜品 tags 三层；忌口 `alg` 只认 15 个 token；`cui` 26 个枚举）。

**高德类型 → 菜系**（`AMAP_CUI_RULES` / `amapCui`）：规则是"拿 type+店名整串从上往下第一个命中"，所以**具体菜系必须排在通用兜底之前**（保定 / 河北 / 家常 / 中餐厅 / 小吃放最后）。2026-09-25 重排过一次，修掉三处判错：胖哥俩（type 浙江菜、店名带保定）→ 原来判成「保定」、粤扬楼淮扬菜（type 淮扬菜）→「家常」、沙县小吃（type 小吃快餐）→「西」；同时补了 浙江/淮扬/苏浙/湘菜/闽菜/徽菜/云贵/拉面/德克士/华莱士 等漏词。**判错的后果不是"显示不准"，而是 `restaurantServes`/`shopCanMake` 认为"这家店做不了那道菜"，Agent 被闸门反复驳回、步数烧完退回算法**（勾「苏浙菜」实测踩过）。改这里务必跑 `test-shop-db.mjs`（末尾有一组真实高德 type 的回归用例）。

## 5. 数据现状（2026-09-25）

- 菜品库 `DISHES`：**811 道**，**26 个菜系**（家常最多，川、粤次之；2026-09-25 新增 `赣` 江西菜）。
- 菜单库 9 家店 331 条：

| 店 | 作用域 | 高德 ID | 菜单 |
| --- | --- | --- | --- |
| 女掌柜土家菜馆 | branch | B0FFGWFUGT | 93 |
| 沙县小吃 | brand | — | 53 |
| 川香苑 | branch | B0K05HVF6L | 26 |
| 熊麻婆现炒浇头面·饭 | branch | B0MUH1QNEC | 30 |
| 胖哥俩肉蟹煲 | brand | B0JUGD3FC6 | 15 |
| 兰湘子·湘菜小炒 | brand | B0K33R6PBI | 26 |
| 小江溪·山野江西菜 | branch | B0L25SCNXX | 26 |
| 麦当劳 | brand | B0FFKKGJSV | 34 |
| 小菜园新徽菜 | brand | B0L0VSHX8E | 28 |

- 参照能覆盖的同城同类店（实测高德）：肉蟹煲类 11 家、湘菜/小炒 76 家、江西菜 12+ 家、汉堡/炸鸡连锁（麦当劳 23 / 肯德基 46 / 汉堡王 4 / 德克士 4 / 华莱士 63）、徽菜店（保定多为安徽牛肉板面馆）。
- 入库流水账 `data\新菜记录.json`：270 条（sx49 / cq69 / cx15 / xm23 / pg15 / lxz22 / xjx26 / mdlxb30 / xcy21）。
- 用户常驻位置：华北电力大学保定二校区 `115.514611,38.888900`。
- 线上：https://zhemgcm-maker.github.io/fanfan-web/
- 队友搭的后端（阿里云 FC）：`https://fanfan-web-qpzmnriewb.cn-hangzhou.fcapp.run`… 实际值见页面设置里的默认后端地址；接口 `/api/llm`（已透传 tools）、`/api/amap`、`/api/register`、`/api/login`、`/api/profile`、`/api/settings`。

## 6. 菜单批量入库（这一波的主线工作）

**快通道脚本**：`开发脚本\ingest-menu.mjs`（2026-09-25 新增，配 `--dry` 预演）

```powershell
$node = "C:\Users\24488\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
& $node 开发脚本\ingest-menu.mjs --menu 菜单.json --kb <正本> --prefix xcy --dry   # 先看清单
& $node 开发脚本\ingest-menu.mjs --menu 菜单.json --kb <正本> --prefix xcy         # 落库（写 DISHES + 解析结果）
& $node 开发脚本\build-data.mjs --parsed 商家数据库\解析结果 --kb <正本> --out data # 不能省：生成 shops.json + 页面快照
```

规矩（用户明确要求过）：

1. **菜名跟库里重叠的，以数据库那份为准**——只把菜单行挂到库里那道菜上（必要时写 `sameAs`），绝不改库里原有的 `cui` / `tags` / 价格。
2. 促销前缀（`10.9元` / `特价` / `甜品站`）和规格后缀（`(单品)`/`(热菜)`）要清；产地前缀（`徽州臭鳜鱼`→`臭鳜鱼`）用 `sameAs` 指过去。
3. `cui` 只能填 26 个枚举（菜单写"徽菜"要转成"徽"、写"西式快餐"要按菜品拆成"汉堡/甜品"）。
4. `alg` 只能填 15 个 token；`"无"` / `"其他"` / `"鸭"` 都不合法，一律清空成 `[]`。
5. **入库后要逐条对账**（拿用户原始菜单比菜品库字段 + 菜单条目 + 价格 + dishId），并把结果原样贴给用户。

## 7. 测试脚本怎么跑

```powershell
& $node 开发脚本\check-syntax.mjs index.html      # 每次必跑：语法 + 重复 id + 缺失 DOM id
& $node 开发脚本\check-structure.mjs index.html   # 标签闭合 / 手机适配 / 无外链
& $node 开发脚本\audit-tags.mjs index.html        # 标签体检：有没有"选不到"的菜、alg 是否合法
& $node 开发脚本\test-shop-db.mjs index.html      # 商家数据库 / 菜系参照 / 高德类型→菜系回归用例
& $node 开发脚本\test-cat-tags.mjs index.html     # 界面标签 → 关键词 → 菜品标签
& $node 开发脚本\test-combo.mjs index.html        # 一桌菜组合逻辑
& $node 开发脚本\test-reroll.mjs index.html       # 换一批 / 不喜欢
& $node 开发脚本\test-locate.mjs index.html       # 定位 / 选地点 / 模糊联想
& $node 开发脚本\test-server.mjs index.html       # 后端融合（离线断言）
& $node 开发脚本\test-memory-edit.mjs index.html  # AI 记忆全记录页 + 删除撤销
& $node 开发脚本\smoke-ui.mjs index.html          # 全交互回归，约 90 秒
& $node 开发脚本\test-agent.mjs index.html        # Agent 真跑（真连 DeepSeek，慢）
& $node 开发脚本\e2e-agent-ui.mjs index.html      # 端到端 + 回退算法引擎
```

`smoke-ui.mjs` 里固定 `agent='off'` 是刻意的（否则真连大模型、结果随机），别改回去。
用户说过：**只加菜单、不碰其他代码时，不用跑全套测试**，跑 `check-syntax` + 一次 `audit-tags` 就够。

## 8. 最近做过什么（时间倒序，细节看 git log）

1. **菜单批量入库 5 家**（2026-09-25）：兰湘子·湘菜小炒（22 新菜）、小江溪·山野江西菜（26）、麦当劳（30）、小菜园新徽菜（21）、胖哥俩肉蟹煲（15）；新增 `ingest-menu.mjs` 快通道和"逐条对账"习惯。为江西菜新增了 `赣` 菜系枚举 + 界面「赣菜」标签；为德克士/华莱士补了类型识别。菜品库 712 → 811 道，菜单库 4 → 9 家。
2. **修「勾苏浙菜 → 步数烧完退回算法」**：根因是高德类型→菜系判错（见第 4 节）。除了重排规则，加了 `can_make` / `anchor_used` / 驳回 hint / 步数将尽提醒四道保险，`AGENT_MAX_STEPS` 12→14。实测同一需求：修复前 12 步压线、14.8 秒、prompt 11.3 万 token、结果还对不上苏浙菜；修复后 7 步、10.2 秒、5.2 万 token，交出「江苏盐水鸭 + 米饭 + 红豆酒 @粤扬楼淮扬菜」。
3. **结果页契合度条修好**：`.track/.fill` 原来是 `<span>`（行内元素 width 不生效），条子一直是空的灰条；现在 `display:block`，条长 = 分数、数字封顶 100。**配色试过"绿/橙/红三档"被用户否掉，已还原成橙色渐变**；分母也还原成 90/20/23/100 —— 别再往"按实测上限校准"或"彩色分档"方向改回去。
4. **点完「就吃这一桌」放「祝你用餐愉快」小特效**（`#bless` 覆盖层，纯 CSS 无外链），2 秒后自动 `exitResultMode()` + `switchTab('home')` 回「今天吃什么」；点一下可跳过。以前记完就停在结果页。
5. **「我的 → AI 记忆」改成三块点进全记录页**：喜欢/不喜欢列全量、逐条可删；被屏蔽的菜和店写出名字、可删可清空；最近吃过按"一顿一张卡"（日期大字 → 饭店名+时刻 → 菜品折叠）。一顿的组标识是 `history[].meal`（`acceptCombo` 写入），老记录按 45 分钟兜底分组；删任何一条 6 秒内可撤销，相对天数按自然日算。
6. 采集入库「兰湘子 / 小菜园」时用户强调的规矩：同名菜冲突以数据库为准、`sameAs` 归并别名不建重复菜。
7. 后端 `/api/llm` 透传 `tools`：页面自动探测 + 自动切换协议，实测原生 function calling 11.5 秒出方案。
8. 选地点支持模糊联想（本地跳字 + 高德 inputtips，300ms 防抖、拼音合成期不发请求）；顺手用高德 POI 校准了三个城市地标坐标。
9. 设置改完即存；状态行标出实际走哪条链路（本机直连 / 后端代理）。

## 9. 待办 / 可能的下一次

- **小江溪 5 道菜的估价待用户确认**：铅山芋子滑牛肉 ¥48、庐陵酱香鸭 ¥58、小炒拆骨肉 ¥49、碎椒炒蛋 ¥26、江西秘制啤酒鸭 ¥58（菜单上没标价，按同类菜估的）。
- 楼下沙县小吃（高德 ID `B0H06MD6IH`，444 米）拍菜单 → 入库 → `bind-shop.mjs` 绑成本店确认。
- 湘菜 / 西北 / 云贵缺"吃饭"类平价菜：实测西北菜 19 道、云贵 10 道里**一道 cat=rice 都没有**，湘菜只有 2 道（都是猪肝）。勾「西北菜 + 吃饭」会空手而归。
- 徽菜参照偏宽：保定的"徽菜"店大多是安徽牛肉板面馆（高德把板面店也标成 安徽菜(徽菜)），它们会被算作能做小菜园那份菜单里的徽菜——这是「菜系参照只做加法」的既定行为，要收紧可以给参照加一层门店类型过滤。
- 若要做「用户自带 Key 走后端（BYOK）」：后端改成"优先用请求头里的 `x-user-key`，没有才用服务端 Key"，前端加一个开关。
- 建议队友：后端 `/api/amap` 的高德 Key 换成「Web服务」类型（现在返 10002，被自动回退本机）。

## 10. 跟这位用户协作的注意事项

- 全程中文；**给结论要给证据**（实测数据、前后对比、失败案例），不要只给判断。
- 在意手机端体验：界面啰嗦、乱码、要一直往下滑这类问题优先级高。
- 文案要朴素，别自作主张加"炫技"的话（例如输入框写"打几个字就联想"被否过）。
- 每次改完的期待是：跑测试 → 推 GitHub → 给线上链接。
- **别擅自改用户给的字段**：入库时把人家给的 `role:'side'` 写成 `single` 被查出来过；数据里明显不合规范的值（`alg:["无"]`、`cui:"西式快餐"`）要改成合法值，但**要在交付说明里写清改了哪些**。
- 用户说过：**加了菜单、没碰别的代码时不用跑全套测试**；**不用每次更新这份交接**（他说"更新交接"再改）。
- 密钥：`DEFAULT_AMAP_KEY`、`DEFAULT_KEY` 明文写在 `index.html` 里，是仓库主的明确选择（后果自负）；**不要在别的文件、日志、文档里再复制这些值**，本仓库是公开的。
