# 标签规范（写菜单数据前看这一页）

菜品库里的 `tags` 不是自由发挥的备注，它同时喂给**打分算法**、**界面筛选**和**大模型**。
写之前先弄清三层结构，就不会出现"写了标签却选不到"的情况。

## 一、三层标签是什么

| 层 | 位置 | 数量 | 谁在用 |
| --- | --- | --- | --- |
| ① 界面标签 | `CAT_CRAVE_TAGS` | 吃饭 17 / 吃面 12 / 其他 15 | 用户在「今天想吃什么」里勾的 chips |
| ② 关键词 | `CRAVE_TAG_KEYS` | 137 个 | 把①翻译成能匹配菜品库的词 |
| ③ 菜品标签 | 每道菜的 `tags` | 库里已用 269 种 | 真正被②匹配的东西 |

例：用户勾「川菜」→ ② 展开成 `川 / 水煮 / 麻辣 / 回锅` → ③ 菜品 `tags` 里有 `川` 就命中。

## 二、匹配有三条通道，权重不同

`craveHits()` 逐关键词地找，命中哪条通道决定得分：

| 通道 | 得分 | 说明 |
| --- | --- | --- |
| 菜名 | 12 | `家常炒面` 里含 `炒面` → 不用写标签也能被「炒面」选中 |
| 菜品标签 | 9 | 主要靠这个 |
| 描述 / 菜系 | 5 | 兜底，容易误伤（见"踩过的坑"） |

⚠️ 单字关键词（目前只有 `炸`）**只认标签**，不看菜名——否则"炸酱面"会被"炸"误伤。

## 三、菜品标签分两类，别搞混

**A 类：参与计算的标签（89 种在用）**——写这些才有用。按用途分组：

- 主料：`猪 牛 羊 鸡 鸭 鱼 虾 蟹 贝 海鲜 蛋 豆 肉 蔬菜 素`
- 口味：`辣 重口 清淡 甜口 甜 酸 酸辣 麻 麻辣 下饭`
- 做法：`汤 干拌 炸 烤 炒 水煮 回锅 清蒸 白灼 涮 烧腊 煲仔 炭火 炖菜 干锅 蒸 煎 拌`
- 场景：`快 聚餐 一人食 小吃 早点 夜宵 下酒 热 凉 暖 解腻`
- 主食形态：`粉 米线 河粉 饺 馄饨 云吞 拉面 刀削 烩面 包子 煎饼 卷饼 热狗 薯条 披萨 汉堡`
- 菜系词：`川 湘 粤 鲁 闽 徽 江浙 西北 东南亚 韩式 日式 西餐 保定 河北 火锅 烤肉 烧烤 甜品 饮品 家常`

**B 类：描述性标签（180 种）**——不影响算分，但会展示给用户、喂给大模型（帮它判断"这家店能不能做"）。
例如 `锅气 香干 卤味 脆爽 软糯 酱香 菌菇 腊肉 猪肝 爆炒 砂锅`。这类可以自由写，**建议写真名，别用"家常菜"这种万能词**。

## 四、写一道菜的推荐骨架

每道菜 **4~8 个标签**，尽量覆盖：主料 1~2 ＋ 做法 1 ＋ 口味 1~2 ＋ 场景 1~2（＋菜系词）。

```js
// 好例子：十八秒猪肝（湘菜盖饭）
{ id:'xm01', name:'十八秒猪肝', cat:'rice', cui:'湘', price:17.9, spicy:2,
  tags:['猪肝','脆爽','爆炒','辣','重口','下饭','快','湘'],
  alg:['猪'], desc:'大火快炒十八秒，猪肝鲜嫩不腥', hot:93 },

// 差例子：只写食材 + 一个万能词
{ tags:['猪肉','家常菜'] }   // 「想吃辣」「想吃肉」「下饭」都选不到它
```

**判断标准**：写完问自己"用户勾哪个标签时应该轮到这道菜？"——把那个标签对应的关键词写进去，就对了。

## 五、另外三个字段的枚举（只能填这些）

**`alg` 忌口食材（15 个 token，别的都不认）**

| 用户勾的忌口 | 要写的 token |
| --- | --- |
| 猪肉 | `猪`（五花肉、腊肉、猪肝、肥肠都归它） |
| 牛肉 / 羊肉 / 鸡肉 | `牛` / `羊` / `鸡` |
| 海鲜/虾蟹 | `海鲜` `虾` `蟹` `鱼` `贝` |
| 鸡蛋 / 豆制品 / 乳制品 / 麸质面食 | `蛋` / `豆` / `乳` / `麦` |
| 花生 / 香菜 | `花生` / `香菜` |

> 写"猪内脏""大豆""小麦"这类**不会被识别**——词表里没有就是没有，等于没忌口。
> 鸭、蛙、螺、红豆等不在词表内的食材，宁可留空，也不要硬塞一个近义词。

**`cui` 菜系（26 个枚举）**：`家常 川 湘 赣 粤 鲁 闽 徽 江浙 东北 西北 云贵 东南亚 日 韩 西 汉堡 披萨 烤肉 烧烤 火锅 甜品 小吃 健康 保定 河北`
（`赣` = 江西菜/赣菜，2026-09-25 为了采「小江溪」加的；界面标签里对应「赣菜」。）
（"台式"归 `家常`，"卤味"归 `小吃`，"饮品"归 `甜品`。）

**`cat` / `role`**：`cat` 只有 `rice`（配饭）/ `noodle`（配面）/ `other`；
`role` 只有 `single 一人食 / main 荤主菜 / side 素菜凉菜 / staple 主食 / soup 汤 / drink 饮品`
（盖饭、拌面这种"一份就是一顿"的不设 role）。

## 六、我踩过的坑（照着躲）

1. **描述里写"生鲜"**：`大丰收`（凉拌时蔬）描述写了"多种生鲜时蔬"→ 被「生鲜/刺身」和「日式料理」选中。生食才写"生鲜/刺身"。
2. **用菜系当口味**：`家常` 只能被「家常菜」这一个界面标签命中，不是"万能兜底"。
3. **标签全写食材**：一排 `['猪肉','青椒','大蒜']` 不解决任何筛选——用户勾的是口味和场景。
4. **忌口写内部词**：见上表，只认 15 个 token。
5. **同名菜重复入库**：菜单叫法和库里不一致时，在解析结果里写 `sameAs` 指过去，别新增一条。

## 七、写完跑一遍体检

```bash
node 开发脚本/audit-tags.mjs index.html                    # 看全库：哪些标签有效、哪些菜标签太弱
node 开发脚本/audit-tags.mjs index.html --menu 我的菜单.json # 入库前体检一份菜单
```

体检会告诉你：每道菜能被几个界面标签选中、有没有"任何标签都选不到"的菜、忌口 token 有没有写错。
