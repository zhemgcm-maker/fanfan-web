# 方案：把「饭饭」从算法引擎改造成 LLM 主导的 Agent

> 目标：解析标签、选菜品、读记忆、反复检索这四件事，由大模型自己决策；
> 算法退到两个位置——**工具**（被 LLM 调用）和**守卫**（校验 LLM 的输出）。

---

## 一、现状与差距

### 现在的分工

| 步骤 | 现在谁做 |
|---|---|
| ① 解析需求 | 算法（读表单） |
| ② 读取记忆 | 算法（读 localStorage） |
| ②b 解析「用一句话说说」 | **LLM**（但只翻译成标签，不决策） |
| ③ 检索菜品库 | 算法（按类别取候选） |
| ④ 过滤 | 算法（忌口/辣度/预算） |
| ⑤ 打分排序 | 算法（8 个加权项） |
| ⑥ 搜真实饭店 | 工具（高德/OSM），但**关键词是算法拼的** |
| ⑦ 组一桌菜 | 算法（锚定菜+主位+汤/饮） |
| ⑧ 按地址选店 | 算法（7 维店评分） |
| ⑨ 写推荐语 | **LLM**（只措辞，不回写结果） |

### 差距在哪

1. LLM 只有「翻译」权，没有「决定」权。它读懂了你想吃汤面，但选哪几道、搜哪个词、去哪家店，全是代码写死的。
2. 没有真正的循环。所有检索一轮完结，搜不到就兜底，不会「换个词再搜一次」。
3. 记忆是死的。用户说「上周吃腻了黄焖鸡」，LLM 听到了也没地方记，下次照样推。
4. 代码里那个 `TOOL_SCHEMA`（4213 行）**是摆设**——只在调试面板里 `JSON.stringify` 显示，从来没传进 API。真正的 `callLLM`（4373 行）发的是写死两条消息的普通对话。

---

## 二、目标架构：三层

```
┌─────────────────────────────────────────────────┐
│  L3 展示层：实时 agent 轨迹 + 可干预             │
│   思考 → 调工具(参数) → 结果摘要 → 再思考 → 定案 │
└─────────────────────────────────────────────────┘
                        ▲
┌─────────────────────────────────────────────────┐
│  L2 智能体循环（harness）                        │
│   拿工具去搜 → 看结果 → 再决定下一步 → finalize  │
│   护栏：步数上限 / 超时 / id 校验 / 失败回退     │
└─────────────────────────────────────────────────┘
                        ▲
┌─────────────────────────────────────────────────┐
│  L1 工具层（全部复用现有代码，薄包装）           │
│   search_dishes / get_profile / search_shops …   │
└─────────────────────────────────────────────────┘
                        ▲
┌─────────────────────────────────────────────────┐
│  L0 数据层：590 道菜 · 商家数据库 · 高德 · 记忆  │
└─────────────────────────────────────────────────┘
```

**核心原则：算法不删，改行当。**
`scoreDish`（2597 行）、`recommendRestaurantsSmart`（2986 行）这些调试了十几轮的逻辑，直接变成 LLM 手上的工具。这样既拿到了 LLM 的推理能力，又保住了「高德真实店铺、采集菜单是事实」这条底线。

---

## 三、工具清单（L1）

| 工具名 | 作用 | 复用的现有函数 |
|---|---|---|
| `search_dishes` | 按关键词/标签/类别/价格搜菜，返回带 id 的候选 | `DISHES`(775) + `inCat`(772) + `craveKeys`(2462) |
| `get_dish` | 单道菜详情（食材、辣度、角色） | `dishById`(1989) |
| `filter_dishes` | 硬性过滤，并**返回被过滤的原因** | `hardFilter`(2832) |
| `rank_dishes` | 多维打分排序（LLM 可当参考，也可不调） | `scoreDish`(2597) |
| `get_profile` | 读记忆：口味、忌口、辣度、点赞点踩、7 天内吃过的 | `state.profile` |
| `remember` | 写一条记忆 | 新增 |
| `search_shops` | 按关键词搜真实店铺 | `amapSearchByKeyword`(2266) / `osmAround`(2339) |
| `get_shop_menu` | 取某店采集到的真实菜单 | `dbMenuDishes`(1953) |
| `list_shops_for_dish` | 某道菜能去哪几家店 | `recommendRestaurantsSmart`(2986) |
| `get_nearby` | 用户周边有什么 | `amapAround`(2309) |
| `build_combo` | 给定店+锚定菜，补齐一桌 | `buildCombo`(3043) |
| `finalize` | 提交终局方案 | 新增（带校验） |

**每个工具的返回值都必须带稳定 id**（菜是 `dish.id`，店是 `shop.id` / 高德 POI ID）。这是防幻觉的地基。

---

## 四、智能体循环（L2，harness）

```
messages = [system(角色 + 硬约束 + 工具说明), user(结构化需求)]

for step in 1..MAX_STEPS(=8):
    r = llmChat(messages, tools=TOOL_SCHEMA, tool_choice='auto')

    if r 有 tool_calls:
        执行工具 → 结果以 role:'tool' 回灌 messages
        continue                      ← 这就是「反复检索」

    if r.content 是 finalize 的 JSON:
        校验（见下）→ 通过就结束渲染

    否则：回一条「格式不对，请按 schema 输出」继续

超步数 / 超时(90s) / 调用失败 → 交给兜底引擎（现有算法全程）
```

### 护栏（必须有，否则一定翻车）

1. **id 白名单闸门**：`finalize` 里的 `shop_id` 和 `dish_ids`，必须出现在本次会话工具返回过的集合里。没出现过 → 拒绝，回一条错误 tool 消息让模型改。这一条直接消灭「编造店名/菜名」。
2. **菜单事实闸门**：采集过菜单的店（`SHOP_DB`），只能点菜单上有的菜；`shopCanMake`(2812) 做裁判。
3. **预算闸门**：整桌合计超过档位上限 → 拒绝，让模型重配。
4. **忌口闸门**：`hardFilter` 结果复检一遍。
5. **步数与配额上限**：`MAX_STEPS`、高德每日调用上限（现有 `amapCallsToday`(2163)）、单次 run 的 token 上限。

### 循环里能长出来的真本事

以前搜不到就是搜不到，现在可以：

- 换词再搜：`川菜` 搜不到 → 自己换成 `水煮鱼` / `川味小炒`；
- 放宽半径：1500m 太少 → 自己扩到 3000m；
- 交叉验证：这家店评分高但菜单对不上 → 换下一家；
- 主动问记忆：不确定你忌口时先 `get_profile`。

---

## 五、记忆怎么改

### 存储：从「几个数组」升级成「记忆条目」

```js
{ id, kind:'preference'|'allergy'|'habit'|'event',
  text:'爱吃肉，尤其是牛肉', evidence:'用户 3 次点牛肉类',
  ts: 1758…, hits: 3, pinned: false }
```

### 三个写入来源

1. 用户手填（「我的」页面的 chips）→ `pinned:true`，最高优先级；
2. LLM 在对话中主动调 `remember`（「你说过不吃香菜」）；
3. 事后复盘：用户点「就吃这个 / 不喜欢」之后，让模型抽一条记忆写进去。

### 读取：每次开局压成一段 brief 注入 system

```
记忆（共 12 条，按重要度）：
- [忌口·固定] 不吃香菜、花生
- [口味·固定] 爱吃肉、微辣
- [习惯] 工作日中午预算 25 以内
- [事件] 9/17 吃过黄焖鸡，评价一般 ← 7 天内别再推
```

这样「读取记忆」就成了 LLM 的一个动作，而不再是代码里一段 if。

---

## 六、要动哪些代码

| 位置 | 现在 | 改成 |
|---|---|---|
| `llmChat` 4327 | 写死两条消息 | 支持完整 messages 数组 + `tools` / `tool_calls` |
| `TOOL_SCHEMA` 4213 | 装饰品 | 真· function calling schema |
| `SYSTEM_PROMPT` 4193 | 「只准复述」型 | 「决策者」型：你可以搜、可以换词、必须 finalize |
| `PARSE_SYSTEM_PROMPT` 4385 | 独立一步 | 被 agent 第一步吸收（保留作轻量路径） |
| `run()` 4534 | 一条流水线 | 拆成 `runAgent()` + `runFallback()`，设置里加开关 |
| `shopIntents` 2413 | 算法拼关键词 | LLM 自己出关键词；这个函数留给兜底 |
| `pickAnchors` 3181 | 算法挑主菜 | 兜底引擎用 |
| 「我的」页面 | 手填 chips | 增加「记忆」区：可看、可删、可置顶 |

---

## 七、代价与风险（必须先接受）

| 项 | 影响 | 对策 |
|---|---|---|
| **延迟** | 一次推荐从 3 秒变 20–60 秒 | 全程流式展示每步；步数上限；工具并行；高德结果已有 10 分钟缓存(2128) |
| **成本** | 一次 run 约 1–2 万输入 token，是现在的 5–10 倍 | 简单场景（只点标签没打字）走轻量路径；按当前单价一次约几分钱，具体以你后台计费为准 |
| **非确定性** | 同样输入两次结果不同 | 建 eval 集（30 条场景）跑 A/B 量化 |
| **幻觉** | 编店名、编菜名 | id 白名单闸门（第四节第 1 条） |
| **离线不可用** | 现在断网还能出结果 | 保留算法兜底，断网自动切换 |
| **Key 暴露** | 现在就是裸奔 | 要收敛就上 Worker（第九节） |
| **推理模型怪癖** | 只返思考链不返结论（已有降级逻辑 4761） | 工具调用阶段也要兜这个错 |
| **高德配额** | LLM 会乱搜，配额跑得更快 | 工具层限流 + 缓存 + 明确告诉模型「今天配额还剩 N 次」 |

---

## 八、实施路线（每期都能独立上线）

| 期 | 内容 | 预估 |
|---|---|---|
| **0** | `llmChat` 升级成通用 messages+tools；TOOL_SCHEMA 变真；设置里加「Agent 模式」开关，随时切回旧引擎 | 半天 |
| **1** | 12 个工具的执行器（全是薄包装）+ 单元测试脚本 | 1 天 |
| **2** | `runAgent()` 循环 + 5 道护栏 + 轨迹流式渲染 + 兜底 | 1 天 |
| **3** | 记忆条目化：注入 brief、`remember` 回写、「我的」页记忆区 | 半天 |
| **4** | 30 条场景 eval：命中率 / 幻觉率 / 耗时 / token，跟旧引擎对比 | 半天 |
| **5**（可选） | Cloudflare Worker 代理：藏 Key、加缓存、以后接你自己的数据库 | 1–2 天 |

**第 0 期就把开关做出来**，所以哪怕新链路不好用，线上随时能切回现在这版。

---

## 九、一个具体例子

输入：小饭 + 吃饭 + 「奶奶做的味道」+ 华电二校区

```
① search_dishes({query:"奶奶做的味道", tags:["家常"], cat:"rice", max_price:25})
   → 12 道：西红柿炒蛋盖饭、土豆炖牛腩、白菜炖豆腐…
② get_profile()
   → 记忆：爱吃肉 / 不吃香菜 / 微辣 / 3 天前吃过黄焖鸡
③ search_shops({keyword:"家常菜", radius_m:1500}) → 8 家
④ search_shops({keyword:"东北菜", radius_m:2000}) → 5 家   ← 模型自己决定换词再搜
⑤ list_shops_for_dish({dish_id:"d123"}) → 命中 2 家
⑥ get_shop_menu({shop_id:"amap-B0H06MD6IH"}) → 菜单 53 道，确认能做
⑦ finalize({shop_id:"amap-B0H…", dish_ids:["d123","s45"], reason:"…"})
   → 校验通过 → 渲染
```

第 ④ 步是整个改造的意义所在：**没有人告诉它要换词，是它自己觉得「家常菜」搜出来的不对，又搜了一次。**

---

## 十、需要你拍板的两件事

1. **要不要后端？**
   - 纯前端（现在这样）：改起来快，GitHub Pages 直接跑；但 Key 继续公开，没法做跨设备记忆，也没有服务端缓存。
   - 加 Worker：能藏 Key、能做真缓存、以后能挂你自己的数据库；代价是多一个部署环节。
   - 建议：第 0–4 期先纯前端，验证效果；好用再上第 5 期。

2. **旧引擎留不留？**
   - 建议留，且默认开着兜底。断网、超步数、模型抽风时直接接管，用户体验不会掉。

---

*文档位置：`D:\饭饭web\方案-LLM主导Agent改造.md`（工作区同步一份）*
